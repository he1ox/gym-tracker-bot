# Idioma y unidades configurables — plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** El bot funciona en español e inglés y muestra los pesos en kg o lb, configurable en un comando nuevo `/settings`, sin convertir ningún número.

**Architecture:** `i18next` con recursos en módulos TypeScript. El idioma, la unidad y el salto de peso del update en curso viven en un módulo global (`src/i18n/current.ts`) que un middleware rellena desde la fila del usuario en cada update. `texts.ts` conserva su API pública (`T.clave`) pero sus propiedades pasan a ser *getters* que delegan en `t()`, así que los diez ficheros que lo consumen no cambian de forma. El único código que genera texto fuera del ciclo de un update, `rest-timer.ts`, captura un `snapshot()` del estado global al programar y lo restaura al disparar.

**Tech Stack:** Node 24 + TypeScript 7, grammY 1.45, `i18next` (nuevo), node:sqlite + drizzle-kit, Vitest 4.

Diseño aprobado: `docs/superpowers/specs/2026-07-25-i18n-and-units-design.md`.

## Global Constraints

- **Idiomas soportados:** `'es'` y `'en'`. Cualquier otro valor es inválido.
- **Idioma de reserva:** `'en'`. `fallbackLng: 'en'`, default de la columna `locale` = `'en'`.
- **Detección en el alta:** `ctx.from?.language_code` que empiece por `es` → `'es'`; cualquier otro valor o ausente → `'en'`.
- **Unidades:** `'kg'` | `'lb'`. Default `'kg'`. **Nunca se convierte ningún número.**
- **Saltos de peso permitidos:** `1`, `2.5`, `5`, `10`. Default `2.5`.
- **`interpolation.escapeValue: false`** en `i18next.init`. Sin esto, los `<b>` y `<code>` de la ayuda se convierten en texto visible.
- **`missingKeyHandler`** lanza si `process.env.NODE_ENV === 'test'`; en otro caso solo `console.error`.
- **`weight_kg` no se renombra** en ninguna tabla.
- **`apps/web` no se toca.** `MUSCLE_GROUP_LABELS` se queda intacto en `packages/core` porque el web lo importa.
- **Comentarios de código en español**, como todo el repo.
- **Trailer de commits:** `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- **Nada de `git push` ni de abrir PRs.** El autor decide cuándo se publica.
- Tests: `pnpm test`. Typecheck: `pnpm typecheck`. Un test concreto: `pnpm vitest run <ruta> -t "<nombre>"`.

---

## Estructura de ficheros

**Nuevos**

| Fichero | Responsabilidad |
|---|---|
| `packages/db/drizzle/0002_*.sql` | Migración: columnas de preferencias + `name_key` + las 51 claves |
| `apps/server/src/i18n/locales/es.ts` | Catálogo español (UI, 17 grupos, 51 ejercicios) |
| `apps/server/src/i18n/locales/en.ts` | Catálogo inglés, mismas claves |
| `apps/server/src/i18n/index.ts` | `initI18n()`, `LOCALES`, tipo `Locale` |
| `apps/server/src/i18n/current.ts` | Estado global del update: `setCurrent`, `snapshot`, `withCurrent`, `t`, `unitLabel`, `weightStep` |
| `apps/server/src/i18n/exercise-name.ts` | `displayName`, `localizeOptions`, `localizeGroups`, `groupLabel` |
| `apps/server/src/i18n/parity.test.ts` | Paridad de claves y variables entre catálogos |
| `apps/server/src/bot/preferences.ts` | Middleware que llama a `setCurrent` |
| `apps/server/src/bot/settings.ts` | Pantalla `/settings` y sus callbacks |
| `apps/server/src/bot/settings.test.ts` | Tests de la pantalla |

**Modificados**

`packages/db/src/schema.ts`, `repositories/users.ts`, `repositories/exercises.ts` · `packages/core/src/set-parser.ts` · `apps/server/src/bot/texts.ts`, `callback-data.ts`, `session-view.ts`, `capture.ts`, `exercise-picker.ts`, `last.ts`, `routines-wizard.ts`, `welcome.ts`, `rest-timer.ts`, `auth.ts`, `bot.ts`, `test-harness.ts` · `apps/server/src/services/session-service.ts`, `exercise-match.ts` · `apps/server/src/main.ts` · `apps/server/package.json` · `CONTRIBUTING.md`

---

## Task 1: Migración 0002 y preferencias en el repositorio de usuarios

**Files:**
- Modify: `packages/db/src/schema.ts:7-12` (tabla `users`), `packages/db/src/schema.ts:14-25` (tabla `exercises`)
- Create: `packages/db/drizzle/0002_<sufijo generado>.sql` (drizzle-kit elige el sufijo)
- Modify: `packages/db/src/repositories/users.ts`
- Test: `packages/db/src/repositories/users.test.ts`, `packages/db/src/migrations.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `UserRow { id, telegramUserId, timezone, locale, weightUnit, weightStep, createdAt }` con `locale: 'es' | 'en'`, `weightUnit: 'kg' | 'lb'`, `weightStep: number`.
  - `createUser(db, { telegramUserId, timezone, locale, createdAt }): UserRow`
  - `updateUserPreferences(db, userId, patch: { locale?: 'es'|'en'; weightUnit?: 'kg'|'lb'; weightStep?: number }): void`
  - Columna `exercises.name_key` (nulable) rellena en las 51 filas del catálogo.

- [ ] **Step 1: Escribir el test que falla (repositorio)**

En `packages/db/src/repositories/users.test.ts`, dentro del `describe` existente:

```ts
it('crea el usuario con los valores por defecto de preferencias', () => {
  const user = createUser(db, {
    telegramUserId: 555,
    timezone: 'Europe/Madrid',
    locale: 'en',
    createdAt: 1000,
  });
  expect(user.locale).toBe('en');
  expect(user.weightUnit).toBe('kg');
  expect(user.weightStep).toBe(2.5);
});

it('actualiza solo las preferencias del patch', () => {
  const user = createUser(db, {
    telegramUserId: 556,
    timezone: 'Europe/Madrid',
    locale: 'es',
    createdAt: 1000,
  });
  updateUserPreferences(db, user.id, { weightUnit: 'lb' });
  const reloaded = getUserByTelegramId(db, 556);
  expect(reloaded?.weightUnit).toBe('lb');
  expect(reloaded?.locale).toBe('es'); // no lo tocaba el patch
  expect(reloaded?.weightStep).toBe(2.5);
});

it('rechaza un idioma fuera del enum', () => {
  const user = createUser(db, {
    telegramUserId: 557,
    timezone: 'Europe/Madrid',
    locale: 'es',
    createdAt: 1000,
  });
  expect(() => db.prepare('UPDATE users SET locale = ? WHERE id = ?').run('fr', user.id)).toThrow();
});
```

Añade `updateUserPreferences` al `import` del principio del fichero.

- [ ] **Step 2: Ejecutar y ver que falla**

Run: `pnpm vitest run packages/db/src/repositories/users.test.ts`
Expected: FAIL — `updateUserPreferences is not exported` / `locale` no existe.

- [ ] **Step 3: Cambiar el esquema**

En `packages/db/src/schema.ts`, sustituye la tabla `users` por:

```ts
export const users = sqliteTable(
  'users',
  {
    id: integer('id').primaryKey({ autoIncrement: true }),
    telegramUserId: integer('telegram_user_id').notNull().unique(),
    timezone: text('timezone').notNull(),
    // Preferencias de presentación. 'en' es el idioma de reserva: es lo que ve
    // quien llega con un language_code desconocido o ausente.
    locale: text('locale', { enum: ['es', 'en'] }).notNull().default('en'),
    // OJO: la unidad es SOLO una etiqueta. Cambiarla no convierte ningún peso ya
    // guardado (decisión del autor), así que el histórico puede mezclar unidades.
    weightUnit: text('weight_unit', { enum: ['kg', 'lb'] }).notNull().default('kg'),
    weightStep: real('weight_step').notNull().default(2.5),
    createdAt: integer('created_at', { mode: 'timestamp_ms' }).notNull(),
  },
  () => [
    check('users_locale_check', sql.raw("locale IN ('es', 'en')")),
    check('users_weight_unit_check', sql.raw("weight_unit IN ('kg', 'lb')")),
    check('users_weight_step_check', sql.raw('weight_step IN (1, 2.5, 5, 10)')),
  ],
);
```

Y en la tabla `exercises` añade, después de `archived`:

```ts
    // Clave de traducción del catálogo base (i18n del bot). NULL en los ejercicios
    // que crea el usuario: esos se muestran con su `name` literal en cualquier idioma.
    nameKey: text('name_key'),
```

- [ ] **Step 4: Generar la migración**

Run: `pnpm --filter @gym-tracker/db generate`
Expected: aparece `packages/db/drizzle/0002_<sufijo>.sql`, una entrada nueva en `drizzle/meta/_journal.json` y un `0002_snapshot.json`.

**Comprueba el SQL generado.** SQLite no sabe añadir un `CHECK` a una tabla existente, así que drizzle genera para `users` la maniobra de tabla nueva + copia + `DROP` + `RENAME`. Es correcta, pero verifica que el `INSERT INTO ... SELECT` copia las filas existentes y que las columnas nuevas reciben su default. Si drizzle no generó el bloque de copia, escríbelo a mano siguiendo este patrón:

```sql
PRAGMA foreign_keys=OFF;--> statement-breakpoint
CREATE TABLE `__new_users` (
	`id` integer PRIMARY KEY AUTOINCREMENT NOT NULL,
	`telegram_user_id` integer NOT NULL,
	`timezone` text NOT NULL,
	`locale` text DEFAULT 'en' NOT NULL,
	`weight_unit` text DEFAULT 'kg' NOT NULL,
	`weight_step` real DEFAULT 2.5 NOT NULL,
	`created_at` integer NOT NULL,
	CONSTRAINT "users_locale_check" CHECK(locale IN ('es', 'en')),
	CONSTRAINT "users_weight_unit_check" CHECK(weight_unit IN ('kg', 'lb')),
	CONSTRAINT "users_weight_step_check" CHECK(weight_step IN (1, 2.5, 5, 10))
);--> statement-breakpoint
INSERT INTO `__new_users`(`id`, `telegram_user_id`, `timezone`, `created_at`)
	SELECT `id`, `telegram_user_id`, `timezone`, `created_at` FROM `users`;--> statement-breakpoint
DROP TABLE `users`;--> statement-breakpoint
ALTER TABLE `__new_users` RENAME TO `users`;--> statement-breakpoint
CREATE UNIQUE INDEX `users_telegram_user_id_unique` ON `users` (`telegram_user_id`);--> statement-breakpoint
PRAGMA foreign_keys=ON;--> statement-breakpoint
ALTER TABLE `exercises` ADD `name_key` text;
```

- [ ] **Step 5: Añadir las 51 claves al final de la migración**

Al final del mismo fichero `.sql`, separado por `--> statement-breakpoint`, añade una sentencia por ejercicio. El criterio (`user_id IS NULL` + nombre exacto) es determinista: el seed de la 0001 insertó estas 51 filas con `user_id NULL`.

```sql
UPDATE `exercises` SET `name_key` = 'bench_press_barbell' WHERE `user_id` IS NULL AND `name` = 'Press de banca con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'incline_press_barbell' WHERE `user_id` IS NULL AND `name` = 'Press inclinado con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'bench_press_dumbbell' WHERE `user_id` IS NULL AND `name` = 'Press de banca con mancuernas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'fly_dumbbell' WHERE `user_id` IS NULL AND `name` = 'Aperturas con mancuernas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'dip_parallel_bars' WHERE `user_id` IS NULL AND `name` = 'Fondos en paralelas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'cable_crossover' WHERE `user_id` IS NULL AND `name` = 'Cruce de poleas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'chest_press_machine' WHERE `user_id` IS NULL AND `name` = 'Press de pecho en máquina';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'overhead_press_barbell' WHERE `user_id` IS NULL AND `name` = 'Press militar con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'shoulder_press_dumbbell' WHERE `user_id` IS NULL AND `name` = 'Press de hombro con mancuernas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'lateral_raise_dumbbell' WHERE `user_id` IS NULL AND `name` = 'Elevaciones laterales con mancuernas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'lateral_raise_cable' WHERE `user_id` IS NULL AND `name` = 'Elevaciones laterales en polea';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'reverse_fly_dumbbell' WHERE `user_id` IS NULL AND `name` = 'Pájaros con mancuernas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'reverse_fly_machine' WHERE `user_id` IS NULL AND `name` = 'Aperturas posteriores en máquina';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'pull_up' WHERE `user_id` IS NULL AND `name` = 'Dominadas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'lat_pulldown' WHERE `user_id` IS NULL AND `name` = 'Jalón al pecho en polea';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'row_barbell' WHERE `user_id` IS NULL AND `name` = 'Remo con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'row_dumbbell_one_arm' WHERE `user_id` IS NULL AND `name` = 'Remo con mancuerna a una mano';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'pullover_cable' WHERE `user_id` IS NULL AND `name` = 'Pullover en polea';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'row_machine' WHERE `user_id` IS NULL AND `name` = 'Remo en máquina';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'row_seated_cable' WHERE `user_id` IS NULL AND `name` = 'Remo sentado en polea';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'face_pull_cable' WHERE `user_id` IS NULL AND `name` = 'Face pull en polea';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'deadlift_conventional' WHERE `user_id` IS NULL AND `name` = 'Peso muerto convencional';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'back_extension' WHERE `user_id` IS NULL AND `name` = 'Hiperextensiones';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'good_morning_barbell' WHERE `user_id` IS NULL AND `name` = 'Buenos días con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'shrug_barbell' WHERE `user_id` IS NULL AND `name` = 'Encogimientos con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'shrug_dumbbell' WHERE `user_id` IS NULL AND `name` = 'Encogimientos con mancuernas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'curl_barbell' WHERE `user_id` IS NULL AND `name` = 'Curl con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'curl_dumbbell' WHERE `user_id` IS NULL AND `name` = 'Curl con mancuernas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'curl_hammer' WHERE `user_id` IS NULL AND `name` = 'Curl martillo';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'curl_preacher' WHERE `user_id` IS NULL AND `name` = 'Curl en banco Scott';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'triceps_pushdown_cable' WHERE `user_id` IS NULL AND `name` = 'Extensión de tríceps en polea';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'skull_crusher_barbell' WHERE `user_id` IS NULL AND `name` = 'Press francés con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'bench_dip' WHERE `user_id` IS NULL AND `name` = 'Fondos entre bancos';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'triceps_extension_overhead' WHERE `user_id` IS NULL AND `name` = 'Extensión de tríceps sobre la cabeza';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'wrist_curl_barbell' WHERE `user_id` IS NULL AND `name` = 'Curl de muñeca con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'wrist_curl_reverse' WHERE `user_id` IS NULL AND `name` = 'Curl de muñeca invertido';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'crunch' WHERE `user_id` IS NULL AND `name` = 'Encogimientos abdominales';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'hanging_leg_raise' WHERE `user_id` IS NULL AND `name` = 'Elevación de piernas colgado';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'plank' WHERE `user_id` IS NULL AND `name` = 'Plancha abdominal';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'ab_wheel_rollout' WHERE `user_id` IS NULL AND `name` = 'Rueda abdominal';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'squat_barbell' WHERE `user_id` IS NULL AND `name` = 'Sentadilla con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'leg_press' WHERE `user_id` IS NULL AND `name` = 'Prensa de piernas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'leg_extension_machine' WHERE `user_id` IS NULL AND `name` = 'Extensión de cuádriceps en máquina';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'lunge_dumbbell' WHERE `user_id` IS NULL AND `name` = 'Zancadas con mancuernas';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'leg_curl_lying' WHERE `user_id` IS NULL AND `name` = 'Curl femoral tumbado';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'romanian_deadlift_barbell' WHERE `user_id` IS NULL AND `name` = 'Peso muerto rumano con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'hip_thrust_barbell' WHERE `user_id` IS NULL AND `name` = 'Hip thrust con barra';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'glute_kickback_cable' WHERE `user_id` IS NULL AND `name` = 'Patada de glúteo en polea';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'hip_adduction_machine' WHERE `user_id` IS NULL AND `name` = 'Aductores en máquina';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'calf_raise_standing' WHERE `user_id` IS NULL AND `name` = 'Elevación de gemelos de pie';--> statement-breakpoint
UPDATE `exercises` SET `name_key` = 'calf_raise_seated' WHERE `user_id` IS NULL AND `name` = 'Elevación de gemelos sentado';
```

- [ ] **Step 6: Actualizar el repositorio de usuarios**

En `packages/db/src/repositories/users.ts`:

```ts
import type { DatabaseSync } from 'node:sqlite';

export type Locale = 'es' | 'en';
export type WeightUnit = 'kg' | 'lb';

export interface UserRow {
  id: number;
  telegramUserId: number;
  timezone: string;
  locale: Locale;
  weightUnit: WeightUnit;
  weightStep: number;
  createdAt: number;
}

interface UserRowDb {
  id: number;
  telegram_user_id: number;
  timezone: string;
  locale: Locale;
  weight_unit: WeightUnit;
  weight_step: number;
  created_at: number;
}

const mapUser = (r: UserRowDb): UserRow => ({
  id: r.id,
  telegramUserId: r.telegram_user_id,
  timezone: r.timezone,
  locale: r.locale,
  weightUnit: r.weight_unit,
  weightStep: r.weight_step,
  createdAt: r.created_at,
});

const SELECT =
  'SELECT id, telegram_user_id, timezone, locale, weight_unit, weight_step, created_at FROM users';

export function getUserByTelegramId(db: DatabaseSync, telegramUserId: number): UserRow | undefined {
  const row = db.prepare(`${SELECT} WHERE telegram_user_id = ?`).get(telegramUserId) as
    | UserRowDb
    | undefined;
  return row ? mapUser(row) : undefined;
}

export function createUser(
  db: DatabaseSync,
  params: { telegramUserId: number; timezone: string; locale: Locale; createdAt: number },
): UserRow {
  const info = db
    .prepare('INSERT INTO users (telegram_user_id, timezone, locale, created_at) VALUES (?, ?, ?, ?)')
    .run(params.telegramUserId, params.timezone, params.locale, params.createdAt);
  return {
    id: Number(info.lastInsertRowid),
    telegramUserId: params.telegramUserId,
    timezone: params.timezone,
    locale: params.locale,
    weightUnit: 'kg',
    weightStep: 2.5,
    createdAt: params.createdAt,
  };
}

export interface UserPreferencesPatch {
  locale?: Locale;
  weightUnit?: WeightUnit;
  weightStep?: number;
}

/** Solo escribe los campos presentes en el patch; un patch vacío no hace nada. */
export function updateUserPreferences(db: DatabaseSync, userId: number, patch: UserPreferencesPatch): void {
  const assignments: string[] = [];
  const values: Array<string | number> = [];
  if (patch.locale !== undefined) {
    assignments.push('locale = ?');
    values.push(patch.locale);
  }
  if (patch.weightUnit !== undefined) {
    assignments.push('weight_unit = ?');
    values.push(patch.weightUnit);
  }
  if (patch.weightStep !== undefined) {
    assignments.push('weight_step = ?');
    values.push(patch.weightStep);
  }
  if (assignments.length === 0) {
    return;
  }
  db.prepare(`UPDATE users SET ${assignments.join(', ')} WHERE id = ?`).run(...values, userId);
}
```

Mantén cualquier otra función que ya exista en el fichero (no borres nada que no aparezca aquí).

- [ ] **Step 7: Añadir `nameKey` al repositorio de ejercicios**

En `packages/db/src/repositories/exercises.ts`: añade `nameKey: string | null` a `ExerciseRow`, `name_key: string | null` a `ExerciseRowDb`, `nameKey: r.name_key` a `mapExercise`, y `name_key` a la constante `SELECT`. En `createCustomExercise` devuelve `nameKey: null` (el `INSERT` no cambia: la columna es nulable).

Añade también `nameKey` a `ExerciseOption` y al `SELECT` de `listExercisesByMuscleGroup`:

```ts
export interface ExerciseOption {
  id: number;
  name: string;
  nameKey: string | null;
}
```

```ts
  const rows = db
    .prepare(
      `SELECT id, name, name_key, muscle_group FROM exercises
       WHERE (user_id IS NULL OR user_id = ?) AND archived = 0
       ORDER BY name COLLATE NOCASE`,
    )
    .all(userId) as unknown as Array<{
      id: number;
      name: string;
      name_key: string | null;
      muscle_group: MuscleGroup;
    }>;
```

Y en los dos `push` del bucle de buckets: `{ id: row.id, name: row.name, nameKey: row.name_key }`.

**Nota sobre el orden:** `ORDER BY name COLLATE NOCASE` sigue ordenando por el nombre español de la BD, no por el traducido. En inglés las listas del selector saldrán en un orden que no es alfabético. Es aceptable y no se aborda aquí; anótalo en el resumen de la tarea.

- [ ] **Step 8: Test de la migración**

En `packages/db/src/migrations.test.ts` añade:

```ts
it('0002 asigna name_key a las 51 filas del catálogo y a ninguna propia', () => {
  const db = openDatabase(':memory:');
  runMigrations(db, MIGRATIONS_DIR);
  const withKey = db
    .prepare('SELECT COUNT(*) AS n FROM exercises WHERE user_id IS NULL AND name_key IS NOT NULL')
    .get() as { n: number };
  const withoutKey = db
    .prepare('SELECT COUNT(*) AS n FROM exercises WHERE user_id IS NULL AND name_key IS NULL')
    .get() as { n: number };
  expect(withKey.n).toBe(51);
  expect(withoutKey.n).toBe(0);
  db.close();
});

it('0002 conserva los usuarios ya existentes al recrear la tabla', () => {
  const db = openDatabase(':memory:');
  // Aplica solo hasta la 0001 no es posible con runMigrations, así que se comprueba
  // el resultado equivalente: un usuario insertado tras migrar mantiene defaults.
  runMigrations(db, MIGRATIONS_DIR);
  db.prepare('INSERT INTO users (telegram_user_id, timezone, created_at) VALUES (1, ?, 0)').run('UTC');
  const row = db.prepare('SELECT locale, weight_unit, weight_step FROM users WHERE telegram_user_id = 1').get();
  expect(row).toEqual({ locale: 'en', weight_unit: 'kg', weight_step: 2.5 });
  db.close();
});
```

Ajusta los `import` a lo que ya use ese fichero de test.

- [ ] **Step 9: Ejecutar los tests de db**

Run: `pnpm vitest run packages/db && pnpm --filter @gym-tracker/db typecheck`
Expected: PASS. Otros paquetes fallarán al compilar porque `createUser` exige `locale` — se arregla en la Task 9; **no lo toques todavía**, `pnpm typecheck` completo aún no pasa.

- [ ] **Step 10: Commit**

```bash
git add packages/db
git commit -m "feat(db): add locale, weight unit and step preferences plus exercise name keys

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 2: i18next y los dos catálogos

**Files:**
- Modify: `apps/server/package.json`
- Create: `apps/server/src/i18n/index.ts`, `apps/server/src/i18n/locales/es.ts`, `apps/server/src/i18n/locales/en.ts`
- Test: `apps/server/src/i18n/parity.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `LOCALES: readonly Locale[]` (con `Locale` importado de `@gym-tracker/db`), `LOCALE_NAMES: Record<Locale, string>`
  - `initI18n(): void`
  - `resources` con el árbol de claves de ambos idiomas.

- [ ] **Step 1: Instalar la dependencia**

Run: `pnpm --filter @gym-tracker/server add i18next`
Expected: `i18next` en `dependencies` de `apps/server/package.json`.

- [ ] **Step 2: Escribir el test de paridad (falla)**

Crea `apps/server/src/i18n/parity.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { en } from './locales/en';
import { es } from './locales/es';

type Tree = { [key: string]: string | Tree };

function flatten(tree: Tree, prefix = ''): Map<string, string> {
  const out = new Map<string, string>();
  for (const [key, value] of Object.entries(tree)) {
    const path = prefix === '' ? key : `${prefix}.${key}`;
    if (typeof value === 'string') {
      out.set(path, value);
    } else {
      for (const [k, v] of flatten(value, path)) {
        out.set(k, v);
      }
    }
  }
  return out;
}

const variables = (value: string): string[] =>
  [...value.matchAll(/\{\{(\w+)\}\}/g)].map((m) => m[1] as string).sort();

describe('paridad de catálogos', () => {
  const flatEs = flatten(es as Tree);
  const flatEn = flatten(en as Tree);

  it('los dos idiomas tienen exactamente las mismas claves', () => {
    expect([...flatEn.keys()].sort()).toEqual([...flatEs.keys()].sort());
  });

  it('cada mensaje usa las mismas variables en los dos idiomas', () => {
    for (const [key, valueEs] of flatEs) {
      expect(variables(valueEs), `variables de ${key}`).toEqual(variables(flatEn.get(key) ?? ''));
    }
  });

  it('ningún valor está vacío', () => {
    for (const [key, value] of [...flatEs, ...flatEn]) {
      expect(value.trim(), `valor de ${key}`).not.toBe('');
    }
  });

  it('cubre los 17 grupos musculares y los 51 ejercicios en los dos idiomas', () => {
    for (const flat of [flatEs, flatEn]) {
      expect([...flat.keys()].filter((k) => k.startsWith('muscleGroup.'))).toHaveLength(17);
      expect([...flat.keys()].filter((k) => k.startsWith('exercise.'))).toHaveLength(51);
    }
  });
});
```

- [ ] **Step 3: Ejecutar y ver que falla**

Run: `pnpm vitest run apps/server/src/i18n/parity.test.ts`
Expected: FAIL — no existen `./locales/es` ni `./locales/en`.

- [ ] **Step 4: Escribir el catálogo español**

Crea `apps/server/src/i18n/locales/es.ts`. Los valores de la sección `ui` son **copia literal** de los strings que hoy están en `apps/server/src/bot/texts.ts` (fichero que sigue en el repo sin cambios hasta la Task 4); los de `muscleGroup` son copia literal de `MUSCLE_GROUP_LABELS` en `packages/core/src/types.ts:23-41`. Lo que cambia de forma respecto a hoy son solo estas claves, que pasan a interpolación de i18next y a plurales:

- `ui.setsCount_one` = `'{{count}} serie'`, `ui.setsCount_other` = `'{{count}} series'` (sustituye a `setsWord` y a `setsSummary`)
- `ui.header` = `'{{icon}} {{title}} · {{sets}} · {{tonnage}} {{unit}}'`
- `ui.recordButton` = `'↻ Registrar {{set}}'`
- `ui.warmupOn` = `'🔥 Calent. ✓'`, `ui.warmupOff` = `'🔥 Calent.'`
- `ui.restCancelButton` = `'⏱ {{seconds}}s ✕'`
- `ui.restDoneMessage` = `'⏱ Descanso terminado — {{exercise}}'`
- `ui.noMatch` = `'No encontré "{{query}}".'`
- `ui.previousRecord` = `' (antes {{weight}} {{unit}})'`
- `ui.routineCreated` = `'Rutina "{{name}}" creada.'`
- `ui.lastNoHistory` = `'Todavía no tienes series de {{name}}.'`
- `ui.lastHeader` = `'📊 {{name}} — últimas sesiones'`
- `ui.lastBest` = `'Mejor 1RM estimado: {{weight}} {{unit}}'`
- `ui.pickGroupTitle` = `'{{label}} — elige un ejercicio:'`
- `ui.welcomeGreetingNamed` = `'👋 Hola, {{name}}'`, `ui.welcomeGreeting` = `'👋 Hola'`
- `ui.weightMinusButton` = `'−{{step}}'`, `ui.weightPlusButton` = `'+{{step}}'`
- `ui.tonnageValue` = `'{{value}} {{unit}}'` (para las filas de la tabla de la bienvenida)

Y las claves nuevas de esta entrega:

```ts
export const es = {
  ui: {
    // ... (todas las claves portadas de texts.ts)
    settingsTitle: '⚙️ Ajustes',
    settingsLanguageLabel: 'Idioma',
    settingsUnitLabel: 'Unidades',
    settingsStepLabel: 'Salto de peso',
    settingsIntro: 'Cambiar de unidad no convierte los pesos que ya registraste.',
    settingsSaved: 'Guardado.',
    welcomeSettingsHint: 'Puedes cambiar el idioma y las unidades en /settings.',
    commandStart: 'Inicio y resumen',
    commandFinish: 'Terminar el entrenamiento',
    commandRoutines: 'Mis rutinas',
    commandLast: 'Historial de un ejercicio',
    commandHelp: 'Cómo funciona',
    commandSettings: 'Idioma y unidades',
  },
  muscleGroup: { chest: 'Pecho', /* … los 17 */ },
  exercise: { bench_press_barbell: 'Press de banca con barra', /* … los 51 */ },
} as const;
```

Los 51 valores españoles son exactamente los nombres del seed; la correspondencia clave→nombre está en la Task 1, Step 5. La línea de `helpText` que lista los comandos debe incluir ahora `<code>/settings</code> — idioma y unidades`.

- [ ] **Step 5: Escribir el catálogo inglés**

Crea `apps/server/src/i18n/locales/en.ts` con el mismo árbol. Traducciones de las claves de `ui` que no son simple espejo:

```
chooseDayPrompt: "What are you training today? Pick a day or train freestyle."
freeWorkoutButton: "🏃 Freestyle"
freeWorkoutTitle: "Freestyle workout"
chooseExercisePrompt: "Pick an exercise:"
otherExerciseButton: "📂 Another exercise"
lastTimeLabel: "Last time"
todayLabel: "Today"
exercisesButton: "☰ Exercises"
repMinusButton: "−1 rep"
repPlusButton: "+1 rep"
weightMinusButton: "−{{step}}"
weightPlusButton: "+{{step}}"
finishTitle: "✅ Workout finished"
sessionEndedToast: "Session ended. Use /start."
noActiveSessionToast: "You have no active session. Use /start."
genericError: "Something went wrong. Try again."
notAuthorized: "You don't have access to this bot."
needWeightAndReps: "Type the weight and the reps, e.g. 60x8."
setsCount_one: "{{count}} set"
setsCount_other: "{{count}} sets"
header: "{{icon}} {{title}} · {{sets}} · {{tonnage}} {{unit}}"
recordButton: "↻ Log {{set}}"
warmupOn: "🔥 Warm-up ✓"
warmupOff: "🔥 Warm-up"
restCancelButton: "⏱ {{seconds}}s ✕"
restDoneMessage: "⏱ Rest over — {{exercise}}"
noMatch: "I couldn't find \"{{query}}\"."
estimatedRecordLabel: "est. 1RM"
previousRecord: " (was {{weight}} {{unit}})"
tonnageValue: "{{value}} {{unit}}"
noRoutines: "You have no routines yet."
routinesList: "Your routines (tap one to activate it):"
newRoutineButton: "➕ New routine"
routineActivated: "Routine activated."
doneButton: "✅ Done"
skipButton: "⏭ Skip"
createOwnButton: "➕ Create my own exercise"
routineAskName: "Name of the new routine:"
routineAskDay: "Type the name of a day (or tap Done to finish):"
routineNeedOneDay: "Add at least one day before finishing."
dayAskExercise: "Search an exercise by name (or tap Done to close the day):"
askOwnName: "Name of your own exercise:"
askMuscleGroup: "Pick the muscle group:"
askTargets: 'Targets like "4 6-10 90" (sets reps-min rest) or tap Skip:'
routineCreated: 'Routine "{{name}}" created.'
lastNoHistory: "You have no sets for {{name}} yet."
lastHeader: "📊 {{name}} — recent sessions"
lastBest: "Best estimated 1RM: {{weight}} {{unit}}"
pickChooseGroup: "Pick a muscle group:"
pickSearchButton: "🔍 Search by name"
pickBackButton: "‹ Back"
pickPrevButton: "‹ Previous"
pickNextButton: "Next ›"
pickByGroupButton: "📂 Browse by group"
pickAmbiguous: "Which one?"
pickEmpty: "No exercises available."
pickTypeName: "Type the exercise name."
lastSearchToast: "To search by name use /last <name>."
exerciseGoneToast: "That exercise is no longer available."
pickGroupTitle: "{{label}} — pick an exercise:"
welcomeIntro: "Log your sets from here: pick a day, pick an exercise and type 60x8."
overviewTitle: "Last 30 days"
overviewCompare: "vs. prev. 30"
overviewWorkouts: "Workouts"
overviewSets: "Sets"
overviewReps: "Reps"
overviewTonnage: "Lifted"
overviewHeaviest: "Heaviest"
overviewNoChange: "—"
welcomeStartButton: "▶️ Start workout"
welcomeRoutinesButton: "📋 My routines"
welcomeHistoryButton: "📊 History"
welcomeHelpButton: "❓ How it works"
welcomeBackButton: "‹ Back"
welcomeGreeting: "👋 Hello"
welcomeGreetingNamed: "👋 Hello, {{name}}"
welcomeSettingsHint: "You can change the language and the units in /settings."
settingsTitle: "⚙️ Settings"
settingsLanguageLabel: "Language"
settingsUnitLabel: "Units"
settingsStepLabel: "Weight step"
settingsIntro: "Switching units does not convert the weights you already logged."
settingsSaved: "Saved."
commandStart: "Home and progress"
commandFinish: "Finish the workout"
commandRoutines: "My routines"
commandLast: "History of an exercise"
commandHelp: "How it works"
commandSettings: "Language and units"
parseNoSetFound: "I didn't get that. Use weight x reps, e.g. 60x8."
parseMissingReps: "The reps are missing, e.g. 60x8."
parseInvalidWeight: "That weight isn't valid."
parseInvalidReps: "Those reps aren't valid."
parseInvalidRpe: "RPE must be between 1 and 10."
```

**Importante en `overviewCompare`:** la tabla de la bienvenida se alinea por caracteres (`welcome.ts:40-43`, ancho total 34). `'vs. prev. 30'` mide 12 y `'vs. 30 anteriores'` mide 17; ambos caben porque el ancho se calcula con `TABLE_WIDTH - overviewCompare.length`. No añadas valores de más de 20 caracteres a esa clave.

`helpText` en inglés: traduce el bloque completo de `texts.ts:135-166` manteniendo **las mismas etiquetas HTML** (`<b>`, `<code>`) y sin introducir ningún `<`, `>` o `&` literal. Los ejemplos (`60x8`, `60 x 8`, `60x8 rpe8`) no se traducen; `sentadilla 100x5` pasa a `squat 100x5`.

Añade también las 17 claves de `muscleGroup`: chest "Chest", front_delt "Front delt", side_delt "Side delt", rear_delt "Rear delt", lats "Lats", upper_back "Upper back", lower_back "Lower back", traps "Traps", biceps "Biceps", triceps "Triceps", forearms "Forearms", abs "Abs", quads "Quads", hamstrings "Hamstrings", glutes "Glutes", adductors "Adductors", calves "Calves".

Y las 51 de `exercise`: bench_press_barbell "Barbell bench press", incline_press_barbell "Incline barbell press", bench_press_dumbbell "Dumbbell bench press", fly_dumbbell "Dumbbell fly", dip_parallel_bars "Parallel bar dip", cable_crossover "Cable crossover", chest_press_machine "Machine chest press", overhead_press_barbell "Barbell overhead press", shoulder_press_dumbbell "Dumbbell shoulder press", lateral_raise_dumbbell "Dumbbell lateral raise", lateral_raise_cable "Cable lateral raise", reverse_fly_dumbbell "Dumbbell reverse fly", reverse_fly_machine "Machine reverse fly", pull_up "Pull-up", lat_pulldown "Lat pulldown", row_barbell "Barbell row", row_dumbbell_one_arm "One-arm dumbbell row", pullover_cable "Cable pullover", row_machine "Machine row", row_seated_cable "Seated cable row", face_pull_cable "Cable face pull", deadlift_conventional "Conventional deadlift", back_extension "Back extension", good_morning_barbell "Barbell good morning", shrug_barbell "Barbell shrug", shrug_dumbbell "Dumbbell shrug", curl_barbell "Barbell curl", curl_dumbbell "Dumbbell curl", curl_hammer "Hammer curl", curl_preacher "Preacher curl", triceps_pushdown_cable "Cable triceps pushdown", skull_crusher_barbell "Barbell skull crusher", bench_dip "Bench dip", triceps_extension_overhead "Overhead triceps extension", wrist_curl_barbell "Barbell wrist curl", wrist_curl_reverse "Reverse wrist curl", crunch "Crunch", hanging_leg_raise "Hanging leg raise", plank "Plank", ab_wheel_rollout "Ab wheel rollout", squat_barbell "Barbell squat", leg_press "Leg press", leg_extension_machine "Machine leg extension", lunge_dumbbell "Dumbbell lunge", leg_curl_lying "Lying leg curl", romanian_deadlift_barbell "Barbell Romanian deadlift", hip_thrust_barbell "Barbell hip thrust", glute_kickback_cable "Cable glute kickback", hip_adduction_machine "Machine hip adduction", calf_raise_standing "Standing calf raise", calf_raise_seated "Seated calf raise".

- [ ] **Step 6: Escribir `initI18n`**

Crea `apps/server/src/i18n/index.ts`:

```ts
import type { Locale } from '@gym-tracker/db';
import i18next from 'i18next';
import { en } from './locales/en';
import { es } from './locales/es';

// El tipo Locale vive en packages/db (Task 1), que es quien lo persiste. Aquí NO
// se redeclara: un segundo tipo con los mismos valores compila pero engaña.
export const LOCALES: readonly Locale[] = ['es', 'en'];

/** El nombre de cada idioma se escribe SIEMPRE en ese idioma, no traducido. */
export const LOCALE_NAMES: Record<Locale, string> = { es: 'Español', en: 'English' };

export const resources = {
  es: { ui: es.ui, muscleGroup: es.muscleGroup, exercise: es.exercise },
  en: { ui: en.ui, muscleGroup: en.muscleGroup, exercise: en.exercise },
};

let initialized = false;

export function initI18n(): void {
  if (initialized) {
    return; // los tests lo llaman por fichero; init dos veces reinicia i18next
  }
  void i18next.init({
    lng: 'en',
    fallbackLng: 'en',
    // 'ui' es el namespace por defecto: t('chooseDayPrompt') sin prefijo.
    ns: ['ui', 'muscleGroup', 'exercise'],
    defaultNS: 'ui',
    resources,
    // CRÍTICO: i18next escapa para HTML de navegador por defecto y convertiría los
    // <b> y <code> de la pantalla de ayuda en texto visible. El escapado de lo que
    // viene del usuario lo sigue haciendo escapeHtml (welcome.ts).
    interpolation: { escapeValue: false },
    saveMissing: true,
    missingKeyHandler: (_lngs, ns, key) => {
      const message = `[i18n] clave sin traducir: ${ns}:${key}`;
      if (process.env.NODE_ENV === 'test') {
        throw new Error(message);
      }
      console.error(message);
    },
  });
  initialized = true;
}
```

- [ ] **Step 7: Ejecutar los tests**

Run: `pnpm vitest run apps/server/src/i18n && pnpm --filter @gym-tracker/server typecheck`
Expected: los cuatro tests de paridad en PASS.

- [ ] **Step 8: Commit**

```bash
git add apps/server/package.json apps/server/src/i18n pnpm-lock.yaml
git commit -m "feat(server): add i18next with Spanish and English catalogs

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 3: Estado global del update y middleware de preferencias

**Files:**
- Create: `apps/server/src/i18n/current.ts`, `apps/server/src/i18n/current.test.ts`, `apps/server/src/bot/preferences.ts`
- Modify: `apps/server/src/bot/bot.ts:27-28`

**Interfaces:**
- Consumes: `initI18n`, `Locale` (Task 2); `UserRow` (Task 1).
- Produces:
  - `type CurrentPrefs = { locale: Locale; unit: WeightUnit; step: number }`
  - `setCurrent(p: CurrentPrefs): void`, `snapshot(): CurrentPrefs`, `withCurrent<T>(p: CurrentPrefs, fn: () => T): T`
  - `t(key: string, vars?: Record<string, unknown>): string`
  - `unitLabel(): string`, `weightStep(): number`, `currentLocale(): Locale`
  - `preferences(): MiddlewareFn<CustomContext>`

- [ ] **Step 1: Escribir el test que falla**

Crea `apps/server/src/i18n/current.test.ts`:

```ts
import { beforeAll, describe, expect, it } from 'vitest';
import { initI18n } from './index';
import { setCurrent, snapshot, t, unitLabel, weightStep, withCurrent } from './current';

describe('estado del update en curso', () => {
  beforeAll(() => initI18n());

  it('traduce con el idioma activo', () => {
    setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
    expect(t('todayLabel')).toBe('Hoy');
    setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
    expect(t('todayLabel')).toBe('Today');
  });

  it('interpola variables y resuelve plurales', () => {
    setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
    expect(t('setsCount', { count: 1 })).toBe('1 serie');
    expect(t('setsCount', { count: 3 })).toBe('3 series');
    setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
    expect(t('setsCount', { count: 1 })).toBe('1 set');
    expect(t('setsCount', { count: 3 })).toBe('3 sets');
  });

  it('expone la unidad y el salto sin convertir nada', () => {
    setCurrent({ locale: 'es', unit: 'lb', step: 5 });
    expect(unitLabel()).toBe('lb');
    expect(weightStep()).toBe(5);
  });

  it('withCurrent restaura el estado anterior incluso si fn lanza', () => {
    setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
    const snap = snapshot();
    setCurrent({ locale: 'es', unit: 'lb', step: 10 });
    expect(() =>
      withCurrent(snap, () => {
        expect(t('todayLabel')).toBe('Today');
        throw new Error('boom');
      }),
    ).toThrow('boom');
    expect(t('todayLabel')).toBe('Hoy'); // el estado de fuera sobrevive
    expect(unitLabel()).toBe('lb');
  });

  it('lanza ante una clave inexistente en test', () => {
    setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
    expect(() => t('clave_que_no_existe')).toThrow(/sin traducir/);
  });
});
```

- [ ] **Step 2: Ejecutar y ver que falla**

Run: `pnpm vitest run apps/server/src/i18n/current.test.ts`
Expected: FAIL — `./current` no existe.

- [ ] **Step 3: Implementar `current.ts`**

```ts
import type { Locale, WeightUnit } from '@gym-tracker/db';
import i18next from 'i18next';

export interface CurrentPrefs {
  locale: Locale;
  unit: WeightUnit;
  step: number;
}

// Estado global del update en curso. Decisión explícita del autor frente a pasar
// las preferencias por parámetro. Riesgo asumido: cualquier código que genere
// texto FUERA del ciclo de vida de un update debe capturar snapshot() al
// programarse y restaurarlo con withCurrent() al ejecutarse, como hace
// rest-timer.ts. Si añades un cron, un webhook o una tarea diferida, haz lo mismo.
let current: CurrentPrefs = { locale: 'en', unit: 'kg', step: 2.5 };

export function setCurrent(prefs: CurrentPrefs): void {
  current = prefs;
}

export function snapshot(): CurrentPrefs {
  return { ...current };
}

export function withCurrent<T>(prefs: CurrentPrefs, fn: () => T): T {
  const previous = current;
  current = prefs;
  try {
    return fn();
  } finally {
    current = previous; // también si fn lanza
  }
}

export const currentLocale = (): Locale => current.locale;
export const unitLabel = (): string => current.unit;
export const weightStep = (): number => current.step;

export function t(key: string, vars?: Record<string, unknown>): string {
  return i18next.getFixedT(current.locale)(key, vars ?? {});
}
```

Si `getFixedT` da problemas de tipos con TypeScript 7, envuelve la llamada en `String(...)`; no uses `any`.

- [ ] **Step 4: Ejecutar y ver que pasa**

Run: `pnpm vitest run apps/server/src/i18n/current.test.ts`
Expected: PASS (5 tests).

- [ ] **Step 5: Escribir el middleware**

Crea `apps/server/src/bot/preferences.ts`:

```ts
import type { MiddlewareFn } from 'grammy';
import { setCurrent } from '../i18n/current';
import type { CustomContext } from './context';

/**
 * Copia las preferencias del usuario al estado global antes de cada update.
 * Va SIEMPRE detrás de auth: necesita ctx.user ya resuelto.
 */
export function preferences(): MiddlewareFn<CustomContext> {
  return async (ctx, next) => {
    setCurrent({
      locale: ctx.user.locale,
      unit: ctx.user.weightUnit,
      step: ctx.user.weightStep,
    });
    await next();
  };
}
```

En `apps/server/src/bot/bot.ts`, justo después de `bot.use(auth(db, config));`:

```ts
  // Detrás de auth: lee ctx.user. Delante de todo lo demás: cualquier handler que
  // pinte texto necesita el idioma ya fijado.
  bot.use(preferences());
```

Con su `import { preferences } from './preferences';`.

- [ ] **Step 6: Verificar typecheck**

Run: `pnpm --filter @gym-tracker/server typecheck`
Expected: sin errores nuevos en `current.ts`, `preferences.ts` ni `bot.ts`. (Los errores por `createUser` sin `locale` en `auth.ts` siguen ahí hasta la Task 9.)

- [ ] **Step 7: Commit**

```bash
git add apps/server/src/i18n/current.ts apps/server/src/i18n/current.test.ts apps/server/src/bot/preferences.ts apps/server/src/bot/bot.ts
git commit -m "feat(server): resolve texts through per-update locale state

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 4: `texts.ts` sobre i18next, sin cambiar sus consumidores

**Files:**
- Modify: `apps/server/src/bot/texts.ts` (reescritura completa)
- Modify: `apps/server/src/bot/test-harness.ts`
- Modify: `apps/server/src/bot/session-view.ts:53,55,62,121-127`
- Test: `apps/server/src/bot/texts.test.ts` (nuevo)

**Interfaces:**
- Consumes: `t`, `unitLabel`, `weightStep` (Task 3); `initI18n` (Task 2).
- Produces: `T` con la **misma superficie pública que hoy** más `T.settings*`, `T.welcomeSettingsHint` y `T.commandDescription(command)`. `parseErrorText(reason)` sin cambios de firma.

- [ ] **Step 1: Escribir el test que falla**

Crea `apps/server/src/bot/texts.test.ts`:

```ts
import { beforeAll, describe, expect, it } from 'vitest';
import { setCurrent } from '../i18n/current';
import { initI18n } from '../i18n/index';
import { T, parseErrorText } from './texts';

describe('T resuelve por idioma activo', () => {
  beforeAll(() => initI18n());

  it('cambia de idioma sin recargar el módulo', () => {
    setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
    expect(T.chooseDayPrompt).toBe('¿Qué toca hoy? Elige un día o entrena libre.');
    setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
    expect(T.chooseDayPrompt).toBe('What are you training today? Pick a day or train freestyle.');
  });

  it('pone la unidad activa en la cabecera, sin convertir el número', () => {
    setCurrent({ locale: 'es', unit: 'lb', step: 2.5 });
    expect(T.header('Torso', 4, '1,250')).toBe('🏋️ Torso · 4 series · 1,250 lb');
  });

  it('rotula los botones de peso con el salto configurado', () => {
    setCurrent({ locale: 'es', unit: 'kg', step: 5 });
    expect(T.weightMinusButton).toBe('−5');
    expect(T.weightPlusButton).toBe('+5');
    setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
    expect(T.weightPlusButton).toBe('+2.5');
  });

  it('traduce los errores del parser', () => {
    setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
    expect(parseErrorText('empty_input')).toBe('Type the weight and the reps, e.g. 60x8.');
    expect(parseErrorText('invalid_rpe')).toBe('RPE must be between 1 and 10.');
  });
});
```

- [ ] **Step 2: Ejecutar y ver que falla**

Run: `pnpm vitest run apps/server/src/bot/texts.test.ts`
Expected: FAIL — `T.chooseDayPrompt` devuelve el literal español en los dos casos.

- [ ] **Step 3: Reescribir `texts.ts`**

Estructura a seguir (aplica el mismo patrón a **todas** las claves que hoy tiene el objeto; no dejes ninguna fuera):

```ts
import type { ParseErrorReason } from '@gym-tracker/core';
import { t, unitLabel, weightStep } from '../i18n/current';

// Formatea el salto sin decimales colgando: 5 → "5", 2.5 → "2.5".
const stepLabel = (): string => String(weightStep());

/**
 * Fachada de textos. Cada propiedad es un getter: se resuelve en el momento de
 * leerla, con el idioma del update en curso (i18n/current.ts). Por eso los
 * consumidores siguen escribiendo `T.clave` igual que antes de la i18n.
 */
export const T = {
  get chooseDayPrompt(): string {
    return t('chooseDayPrompt');
  },
  get freeWorkoutButton(): string {
    return t('freeWorkoutButton');
  },
  // … el resto de propiedades estáticas, una por clave

  get weightMinusButton(): string {
    return t('weightMinusButton', { step: stepLabel() });
  },
  get weightPlusButton(): string {
    return t('weightPlusButton', { step: stepLabel() });
  },

  header(dayName: string | null, effectiveSets: number, tonnage: string): string {
    return t('header', {
      icon: t('headerIcon'),
      title: dayName ?? t('freeWorkoutTitle'),
      sets: t('setsCount', { count: effectiveSets }),
      tonnage,
      unit: unitLabel(),
    });
  },
  setsSummary(count: number): string {
    return t('setsCount', { count });
  },
  recordButton(label: string): string {
    return t('recordButton', { set: label });
  },
  warmupButton(on: boolean): string {
    return on ? t('warmupOn') : t('warmupOff');
  },
  restCancelButton(seconds: number): string {
    return t('restCancelButton', { seconds });
  },
  restDoneMessage(exerciseName: string): string {
    return t('restDoneMessage', { exercise: exerciseName });
  },
  noMatch(query: string): string {
    return t('noMatch', { query });
  },
  previousRecord(weight: string): string {
    return t('previousRecord', { weight, unit: unitLabel() });
  },
  routineCreated(name: string): string {
    return t('routineCreated', { name });
  },
  lastNoHistory(name: string): string {
    return t('lastNoHistory', { name });
  },
  lastHeader(name: string): string {
    return t('lastHeader', { name });
  },
  lastBest(weight: string): string {
    return t('lastBest', { weight, unit: unitLabel() });
  },
  pickGroupTitle(label: string): string {
    return t('pickGroupTitle', { label });
  },
  welcomeGreeting(firstName: string | null): string {
    return firstName === null ? t('welcomeGreeting') : t('welcomeGreetingNamed', { name: firstName });
  },
  // Valor con unidad para las filas de la tabla de la bienvenida.
  withUnit(value: string): string {
    return t('tonnageValue', { value, unit: unitLabel() });
  },
  get helpText(): string {
    return t('helpText');
  },
  // Descripciones del menú ☰ (una clave por comando).
  commandDescription(command: 'start' | 'finish' | 'routines' | 'last' | 'help' | 'settings'): string {
    const keys = {
      start: 'commandStart',
      finish: 'commandFinish',
      routines: 'commandRoutines',
      last: 'commandLast',
      help: 'commandHelp',
      settings: 'commandSettings',
    } as const;
    return t(keys[command]);
  },
} as const;

export function parseErrorText(reason: ParseErrorReason): string {
  switch (reason) {
    case 'empty_input':
      return t('needWeightAndReps');
    case 'no_set_found':
      return t('parseNoSetFound');
    case 'missing_reps':
      return t('parseMissingReps');
    case 'invalid_weight':
      return t('parseInvalidWeight');
    case 'invalid_reps':
      return t('parseInvalidReps');
    case 'invalid_rpe':
      return t('parseInvalidRpe');
  }
}
```

Borra la función local `setsWord` y la constante `MUSCLE_GROUP_LABELS` no se toca (vive en `core`).

- [ ] **Step 4: Sustituir los ` kg` de `session-view.ts`**

En `renderFinishSummary` (líneas 117-130) usa `T.withUnit(...)` en lugar de concatenar `' kg'`:

```ts
export function renderFinishSummary(summary: FinishSummary): string {
  const title = summary.dayName ?? T.freeWorkoutTitle;
  const lines = [
    T.finishTitle,
    `${title} · ${T.setsSummary(summary.effectiveSets)} · ${T.withUnit(
      formatTonnage(summary.tonnageKg),
    )} · ${summary.durationMinutes} min`,
  ];
  for (const rec of summary.records) {
    const previous = rec.previous1RM === null ? '' : T.previousRecord(format1RM(rec.previous1RM));
    lines.push(
      `${T.recordIcon} ${rec.exerciseName}: ${T.withUnit(format1RM(rec.estimated1RM))} ${T.estimatedRecordLabel}${previous}`,
    );
  }
  return lines.join('\n');
}
```

`formatWeight`, `formatSet`, `formatTonnage` y `format1RM` **no cambian**: son números puros, sin unidad.

- [ ] **Step 5: Fijar el idioma en el harness de tests**

Dos cambios en `apps/server/src/bot/test-harness.ts`, y el segundo es imprescindible.

Primero, dentro de `makeHarness`, antes de `createBot`:

```ts
  // Estado por defecto para los renders que se invocan fuera de un update.
  initI18n();
  setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
```

Con `import { initI18n } from '../i18n/index';` y `import { setCurrent } from '../i18n/current';`.

Segundo — y esto es lo que de verdad decide el idioma de los tests: el middleware
`preferences()` **sobrescribe ese estado en cada update** con lo que haya en la fila del
usuario, y `detectLocale(undefined)` da `'en'`. Sin tocar el harness, los 276 tests
existentes empezarían a ver inglés. Los updates del harness tienen que declararse en
español, en los dos constructores:

En `textUpdate` (línea 75), el `from` pasa a ser:

```ts
      // language_code: los tests existentes se escribieron contra los textos en
      // español, y es este campo —no setCurrent— el que decide el idioma del alta.
      from: { id: fromId, is_bot: false, first_name: 'U', language_code: 'es' },
```

Y exactamente el mismo `from` en `callbackUpdate` (línea 102).

Los tests de render puro que no usan el harness (`session-view.test.ts`, `welcome.test.ts`, `exercise-picker.test.ts`, `last.test.ts`, `flows.test.ts`, `routines-wizard.test.ts`) necesitan lo mismo en un `beforeAll`. Añádelo a los que fallen.

- [ ] **Step 6: Ejecutar toda la suite del server**

Run: `pnpm vitest run apps/server && pnpm --filter @gym-tracker/server typecheck`
Expected: PASS. Si algún test compara un texto que ahora lleva la unidad por `withUnit`, comprueba que el valor esperado sigue siendo el mismo string que antes (`"… 1,250 kg"`): no debería cambiar ni un carácter con `unit: 'kg'`.

- [ ] **Step 7: Commit**

```bash
git add apps/server/src/bot/texts.ts apps/server/src/bot/texts.test.ts apps/server/src/bot/session-view.ts apps/server/src/bot/test-harness.ts apps/server/src/bot
git commit -m "refactor(server): resolve T through i18next keys

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 5: Contener el estado global en el aviso de descanso

**Files:**
- Modify: `apps/server/src/bot/rest-timer.ts`
- Test: `apps/server/src/bot/rest-timer.test.ts`

**Interfaces:**
- Consumes: `snapshot`, `withCurrent`, `setCurrent` (Task 3).
- Produces: `RestTimers` con la misma firma pública.

- [ ] **Step 1: Escribir el test que falla**

En `apps/server/src/bot/rest-timer.test.ts`:

```ts
it('usa el idioma de cuando se programó, no el del update que llegue después', async () => {
  vi.useFakeTimers();
  initI18n();
  const sent: string[] = [];
  const timers = createRestTimers({
    send: async (_chatId, text) => {
      sent.push(text);
      return 1;
    },
    storeEphemeral: () => {},
    rerender: async () => {},
  });

  setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
  timers.schedule({ userId: 1, chatId: 10, seconds: 60, exerciseName: 'Sentadilla con barra' });

  // Entre programar y disparar entra un update de otro idioma.
  setCurrent({ locale: 'en', unit: 'lb', step: 5 });

  await vi.advanceTimersByTimeAsync(60_000);
  expect(sent).toEqual(['⏱ Descanso terminado — Sentadilla con barra']);
  vi.useRealTimers();
});
```

Añade los `import` de `vi`, `initI18n` y `setCurrent` que falten.

- [ ] **Step 2: Ejecutar y ver que falla**

Run: `pnpm vitest run apps/server/src/bot/rest-timer.test.ts -t "idioma de cuando se programó"`
Expected: FAIL — llega `'⏱ Rest over — …'`.

- [ ] **Step 3: Capturar y restaurar el estado**

En `rest-timer.ts`:

```ts
import { snapshot, withCurrent } from '../i18n/current';
import { T } from './texts';
```

En `schedule`, antes del `setTimeout`:

```ts
      // El aviso se construye MÁS TARDE, fuera del ciclo de vida de este update:
      // si leyera el estado global en ese momento cogería el idioma de cualquier
      // otro update que hubiera entrado en medio. Se congela aquí.
      const prefs = snapshot();
```

Y dentro del callback, sustituye la línea del `send` por:

```ts
            const messageId = await deps.send(chatId, withCurrent(prefs, () => T.restDoneMessage(exerciseName)));
```

`deps.rerender` repinta el mensaje activo y se ejecuta ya fuera de `withCurrent`: eso es correcto, porque el mensaje de sesión debe pintarse con el idioma **actual** del usuario, no con el de hace tres minutos.

- [ ] **Step 4: Ejecutar y ver que pasa**

Run: `pnpm vitest run apps/server/src/bot/rest-timer.test.ts`
Expected: PASS, todos los tests del fichero.

- [ ] **Step 5: Commit**

```bash
git add apps/server/src/bot/rest-timer.ts apps/server/src/bot/rest-timer.test.ts
git commit -m "fix(server): freeze locale when scheduling the rest reminder

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 6: Unidad y salto configurable en la captura

**Files:**
- Modify: `packages/core/src/set-parser.ts:21`
- Modify: `apps/server/src/bot/callback-data.ts:3,43-58,121-128`
- Modify: `apps/server/src/bot/capture.ts:349-354`
- Modify: `apps/server/src/bot/welcome.ts:70-73`
- Test: `packages/core/src/set-parser.test.ts`, `apps/server/src/bot/callback-data.test.ts`, `apps/server/src/bot/welcome.test.ts`

**Interfaces:**
- Consumes: `weightStep`, `unitLabel` (Task 3); `T.withUnit` (Task 4).
- Produces: `CallbackAction` con `{ type: 'weight'; direction: 1 | -1 }` y `{ type: 'reps'; delta: number }` (reps no cambia). `WEIGHT_STEP` **desaparece**.

- [ ] **Step 1: Escribir los tests que fallan**

En `packages/core/src/set-parser.test.ts`:

```ts
it('acepta el sufijo lb o lbs y no convierte el número', () => {
  expect(parseSetInput('100lb x 8')).toEqual({ ok: true, value: { exerciseName: '', weightKg: 100, reps: 8, rpe: undefined } });
  expect(parseSetInput('100lbs x 8')).toEqual({ ok: true, value: { exerciseName: '', weightKg: 100, reps: 8, rpe: undefined } });
  expect(parseSetInput('100kg x 8')).toEqual({ ok: true, value: { exerciseName: '', weightKg: 100, reps: 8, rpe: undefined } });
});
```

Ajusta la forma del objeto esperado a la que ya usen los tests vecinos de ese fichero (mira uno antes de escribirlo).

En `apps/server/src/bot/callback-data.test.ts`:

```ts
it('el ajuste de peso devuelve dirección, no incremento', () => {
  expect(parseCallback('w+')).toEqual({ type: 'weight', direction: 1 });
  expect(parseCallback('w-')).toEqual({ type: 'weight', direction: -1 });
});
```

Y sustituye cualquier aserción existente que esperase `delta: 2.5` / `delta: -2.5` para `w+` / `w-`.

- [ ] **Step 2: Ejecutar y ver que fallan**

Run: `pnpm vitest run packages/core/src/set-parser.test.ts apps/server/src/bot/callback-data.test.ts`
Expected: FAIL en los dos ficheros.

- [ ] **Step 3: Ampliar el sufijo del parser**

En `packages/core/src/set-parser.ts` línea 21, cambia `(?:kg)?` por `(?:kg|lbs?)?`, y actualiza el comentario de la línea 18:

```ts
// [nombre] peso [kg|lb|lbs] x reps [rpe N] — siempre peso primero, tolerante a
// espacios. El sufijo de unidad se IGNORA: nunca convierte (decisión del autor),
// solo evita que "100kg x 8" o "100lb x 8" se lean como error de formato.
```

- [ ] **Step 4: Cambiar el contrato del callback de peso**

En `apps/server/src/bot/callback-data.ts`: borra `export const WEIGHT_STEP = 2.5;` (deja `REP_STEP`), cambia la variante a `| { type: 'weight'; direction: 1 | -1 }` y en el `switch`:

```ts
    case CB.wPlus:
      // Solo la dirección: el incremento es una preferencia del usuario y este
      // parser debe seguir siendo puro (sin leer el estado global de i18n).
      return { type: 'weight', direction: 1 };
    case CB.wMinus:
      return { type: 'weight', direction: -1 };
```

En `apps/server/src/bot/capture.ts` línea 349-351:

```ts
    case 'weight':
      adjustPending(db, { session, weightDelta: action.direction * weightStep(), now });
      break;
```

con `import { weightStep } from '../i18n/current';`.

Comprueba con `grep -rn "WEIGHT_STEP" apps packages --include=*.ts` que no queda ningún uso.

- [ ] **Step 5: Poner la unidad en la tabla de la bienvenida**

En `apps/server/src/bot/welcome.ts`, líneas 70-73:

```ts
    metricRow(T.overviewTonnage, T.withUnit(formatTonnage(overview.tonnageKg.current)), overview.tonnageKg.changePercent),
    // formatWeight, NO formatTonnage: es el peso crudo de una serie y redondearlo
    // convertiría 62.5 kg en 63 kg, que es un dato falso.
    metricRow(T.overviewHeaviest, T.withUnit(formatWeight(overview.heaviest.current)), overview.heaviest.changePercent),
```

Y en `apps/server/src/bot/last.ts`, sustituye la concatenación de `' kg'` de `T.lastBest(...)` por la clave con unidad (ya la incluye `T.lastBest`, así que basta pasarle el número formateado sin unidad).

- [ ] **Step 6: Test de la tabla en libras**

En `apps/server/src/bot/welcome.test.ts`:

```ts
it('pinta la tabla con la unidad activa y sin descuadrar', () => {
  setCurrent({ locale: 'es', unit: 'lb', step: 2.5 });
  const { text } = renderWelcome(model); // reutiliza el modelo del test vecino
  expect(text).toContain('lb');
  expect(text).not.toContain('kg');
  const rows = text.split('<pre>')[1]?.split('</pre>')[0]?.split('\n') ?? [];
  const widths = new Set(rows.slice(0, 6).map((r) => r.length));
  expect(widths.size).toBe(1); // todas las filas de la tabla miden lo mismo
});
```

- [ ] **Step 7: Ejecutar todo**

Run: `pnpm test && pnpm typecheck`
Expected: los tests de `core`, `db` y `server` en PASS. `apps/web` no se ha tocado y sigue igual. (Si `typecheck` falla en `auth.ts` por `createUser`, esa es la Task 9.)

- [ ] **Step 8: Commit**

```bash
git add packages/core apps/server/src/bot
git commit -m "feat(server): honour the configured unit label and weight step

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 7: Nombres de ejercicio y grupos musculares traducidos

**Files:**
- Create: `apps/server/src/i18n/exercise-name.ts`, `apps/server/src/i18n/exercise-name.test.ts`
- Modify: `apps/server/src/bot/exercise-picker.ts:1,36,82`, `apps/server/src/bot/routines-wizard.ts:279`
- Modify: `apps/server/src/services/session-service.ts:170,178,229,251`
- Modify: `apps/server/src/bot/capture.ts:107-119`, `apps/server/src/bot/last.ts:29,79`
- Modify: `apps/server/src/services/exercise-match.ts`
- Test: `apps/server/src/services/exercise-match.test.ts`, `apps/server/src/bot/exercise-picker.test.ts`

**Interfaces:**
- Consumes: `t`, `currentLocale` (Task 3); `ExerciseRow`/`ExerciseOption` con `nameKey` (Task 1).
- Produces:
  - `displayName(row: { name: string; nameKey: string | null }): string`
  - `localizeOptions(options: readonly ExerciseOption[]): ExerciseOption[]` — devuelve copias con `name` ya traducido
  - `localizeGroups(map): Map<MuscleGroup, ExerciseOption[]>`
  - `groupLabel(group: MuscleGroup): string`
  - `matchExercise` recibe además el nombre original: pool de `{ id; name; nameKey }`.

- [ ] **Step 1: Escribir el test que falla**

Crea `apps/server/src/i18n/exercise-name.test.ts`:

```ts
import { beforeAll, describe, expect, it } from 'vitest';
import { setCurrent } from './current';
import { displayName, groupLabel, localizeOptions } from './exercise-name';
import { initI18n } from './index';

describe('nombres traducibles', () => {
  beforeAll(() => initI18n());

  it('traduce los del catálogo por su clave', () => {
    setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
    expect(displayName({ name: 'Sentadilla con barra', nameKey: 'squat_barbell' })).toBe('Barbell squat');
    setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
    expect(displayName({ name: 'Sentadilla con barra', nameKey: 'squat_barbell' })).toBe('Sentadilla con barra');
  });

  it('deja intactos los ejercicios propios', () => {
    setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
    expect(displayName({ name: 'Mi invento raro', nameKey: null })).toBe('Mi invento raro');
  });

  it('traduce los grupos musculares', () => {
    setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
    expect(groupLabel('chest')).toBe('Chest');
    setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
    expect(groupLabel('chest')).toBe('Pecho');
  });

  it('localizeOptions no muta la entrada', () => {
    setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
    const input = [{ id: 1, name: 'Dominadas', nameKey: 'pull_up' }];
    const output = localizeOptions(input);
    expect(output[0]?.name).toBe('Pull-up');
    expect(input[0]?.name).toBe('Dominadas');
  });
});
```

- [ ] **Step 2: Ejecutar y ver que falla**

Run: `pnpm vitest run apps/server/src/i18n/exercise-name.test.ts`
Expected: FAIL — el módulo no existe.

- [ ] **Step 3: Implementar el módulo**

```ts
import type { MuscleGroup } from '@gym-tracker/core';
import type { ExerciseOption } from '@gym-tracker/db';
import { t } from './current';

export interface Nameable {
  name: string;
  nameKey: string | null;
}

/**
 * Nombre a mostrar. Los del catálogo base tienen clave y se traducen; los que crea
 * el usuario (nameKey NULL) se muestran tal cual los escribió, en cualquier idioma.
 */
export function displayName(row: Nameable): string {
  return row.nameKey === null ? row.name : t(`exercise:${row.nameKey}`);
}

export function localizeOptions(options: readonly ExerciseOption[]): ExerciseOption[] {
  return options.map((option) => ({ ...option, name: displayName(option) }));
}

export function localizeGroups(
  byGroup: ReadonlyMap<MuscleGroup, readonly ExerciseOption[]>,
): Map<MuscleGroup, ExerciseOption[]> {
  const out = new Map<MuscleGroup, ExerciseOption[]>();
  for (const [group, options] of byGroup) {
    out.set(group, localizeOptions(options)); // conserva el orden de inserción
  }
  return out;
}

export const groupLabel = (group: MuscleGroup): string => t(`muscleGroup:${group}`);
```

- [ ] **Step 4: Ejecutar y ver que pasa**

Run: `pnpm vitest run apps/server/src/i18n/exercise-name.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Usar `groupLabel` en el bot**

En `apps/server/src/bot/exercise-picker.ts`: quita `MUSCLE_GROUP_LABELS` del import de `@gym-tracker/core` (deja `MUSCLE_GROUPS` y el tipo), importa `groupLabel` de `../i18n/exercise-name`, y sustituye `MUSCLE_GROUP_LABELS[group]` por `groupLabel(group)` en las líneas 36 y 82.

Lo mismo en `apps/server/src/bot/routines-wizard.ts:279`.

En `apps/server/src/bot/exercise-picker.test.ts`, sustituye `MUSCLE_GROUP_LABELS.chest` por `groupLabel('chest')` (con un `beforeAll` que llame a `initI18n()` y fije el idioma español).

**No toques `MUSCLE_GROUP_LABELS` en `packages/core`**: `apps/web` lo importa en cinco sitios.

- [ ] **Step 6: Localizar los nombres en los tres puntos de entrada**

En `apps/server/src/bot/capture.ts`, en `showPicker`:

```ts
  const { text, keyboard } = renderPicker(state, 'c', localizeGroups(listExercisesByMuscleGroup(db, session.userId)));
```

En `apps/server/src/bot/welcome.ts`, en el handler de `CB.wcHistory`:

```ts
    const picker = renderPicker({ view: 'groups' }, 'l', localizeGroups(listExercisesByMuscleGroup(db, ctx.user.id)));
```

En `apps/server/src/bot/last.ts` y en cualquier otro `renderPicker(...)` que quede: envuelve igual. Comprueba con `grep -rn "listExercisesByMuscleGroup" apps/server/src` que **todas** las llamadas pasan por `localizeGroups`.

En `apps/server/src/services/session-service.ts`, sustituye por `displayName(...)`:
- línea 170: `name: displayName(detail)` — requiere que `listRoutineExerciseDetails` devuelva `nameKey`; si no lo devuelve, añade `name_key` a su `SELECT` y a su tipo en `packages/db/src/repositories/routines.ts`.
- línea 178: `name: displayName(exercise)`
- línea 229: `exerciseName: exercise ? displayName(exercise) : ''`
- línea 251: `exerciseName: exercise ? displayName(exercise) : ''`

En `apps/server/src/bot/last.ts:29`: `const name = exercise ? displayName(exercise) : '';`

- [ ] **Step 7: Buscar por los dos nombres**

Test primero, en `apps/server/src/services/exercise-match.test.ts`:

```ts
it('encuentra por el nombre traducido y por el original', () => {
  setCurrent({ locale: 'en', unit: 'kg', step: 2.5 });
  const pool = [{ id: 1, name: 'Sentadilla con barra', nameKey: 'squat_barbell' }];
  expect(matchExercise('squat', pool)).toEqual({ kind: 'unique', exercise: pool[0] });
  // El usuario que lleva meses escribiendo "sentadilla" no debe perder el atajo.
  expect(matchExercise('sentadilla', pool)).toEqual({ kind: 'unique', exercise: pool[0] });
});
```

Implementación en `apps/server/src/services/exercise-match.ts`: cambia la restricción del genérico a `T extends { name: string; nameKey?: string | null }` y construye, para cada candidato, la lista de nombres a comparar:

```ts
import { displayName } from '../i18n/exercise-name';

// Cada ejercicio se busca por su nombre mostrado Y por el de la BD: quien lleva
// meses escribiendo "sentadilla" no pierde el atajo al pasar el bot a inglés.
const namesOf = (e: { name: string; nameKey?: string | null }): string[] => {
  const shown = displayName({ name: e.name, nameKey: e.nameKey ?? null });
  return shown === e.name ? [e.name] : [shown, e.name];
};
```

Y en las dos pasadas (exacta y por términos) usa `namesOf(e).some(...)` en lugar de comparar solo `normalize(e.name)`. Mantén la prioridad: primero coincidencia exacta de nombre completo, después por términos.

En `capture.ts`, `poolForMatching` debe propagar `nameKey` en los tres `map` (`{ id: e.id, name: e.name, nameKey: e.nameKey }`), y `last.ts:79` igual. `resolveExerciseByName` devuelve el nombre **ya mostrado**: `name: displayName(result.exercise)`.

- [ ] **Step 8: Ejecutar toda la suite**

Run: `pnpm test && pnpm typecheck`
Expected: PASS en `core`, `db` y `server`.

- [ ] **Step 9: Commit**

```bash
git add apps/server/src packages/db/src
git commit -m "feat(server): translate catalog exercise and muscle group names

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 8: La pantalla `/settings`

**Files:**
- Create: `apps/server/src/bot/settings.ts`, `apps/server/src/bot/settings.test.ts`
- Modify: `apps/server/src/bot/callback-data.ts`
- Modify: `apps/server/src/bot/bot.ts` (registrar antes de `registerCapture`)

**Interfaces:**
- Consumes: `updateUserPreferences` (Task 1); `LOCALES`, `LOCALE_NAMES` (Task 2); `setCurrent` (Task 3); `T.settings*` (Task 4).
- Produces:
  - `WEIGHT_STEPS: readonly [1, 2.5, 5, 10]`
  - `renderSettings(prefs: { locale: Locale; unit: WeightUnit; step: number }): { text: string; keyboard: InlineKeyboard }`
  - `registerSettings(bot: Bot<CustomContext>, db: DatabaseSync): void`
  - `CB.setLocale(l)`, `CB.setUnit(u)`, `CB.setStep(s)`

- [ ] **Step 1: Escribir el test que falla**

Crea `apps/server/src/bot/settings.test.ts`:

```ts
import { openDatabase, MIGRATIONS_DIR, runMigrations, getUserByTelegramId } from '@gym-tracker/db';
import type { DatabaseSync } from 'node:sqlite';
import { afterEach, beforeAll, beforeEach, describe, expect, it } from 'vitest';
import { setCurrent } from '../i18n/current';
import { initI18n } from '../i18n/index';
import { commandUpdate, callbackUpdate, lastKeyboardDatas, makeHarness, outgoingTexts, BOT_INFO } from './test-harness';
import { renderSettings } from './settings';

const config = { allowedTelegramIds: [111], timezone: 'Europe/Madrid' };

describe('renderSettings', () => {
  beforeAll(() => initI18n());

  it('marca con ✓ la opción activa de cada fila', () => {
    setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
    const { text, keyboard } = renderSettings({ locale: 'es', unit: 'kg', step: 2.5 });
    const labels = keyboard.inline_keyboard.flat().map((b) => ('text' in b ? b.text : ''));
    expect(text).toContain('⚙️ Ajustes');
    expect(labels).toContain('✓ Español');
    expect(labels).toContain('English');
    expect(labels).toContain('✓ kg');
    expect(labels).toContain('lb');
    expect(labels).toContain('✓ 2.5');
  });

  it('se pinta en el idioma activo', () => {
    setCurrent({ locale: 'en', unit: 'lb', step: 5 });
    const { keyboard, text } = renderSettings({ locale: 'en', unit: 'lb', step: 5 });
    const labels = keyboard.inline_keyboard.flat().map((b) => ('text' in b ? b.text : ''));
    expect(text).toContain('⚙️ Settings');
    expect(labels).toContain('✓ English');
    expect(labels).toContain('✓ lb');
    expect(labels).toContain('✓ 5');
  });
});

describe('/settings', () => {
  let db: DatabaseSync;
  beforeAll(() => initI18n());
  beforeEach(() => {
    db = openDatabase(':memory:');
    runMigrations(db, MIGRATIONS_DIR);
  });
  afterEach(() => db.close());

  it('responde con un mensaje NUEVO, no editando', async () => {
    const { bot, outgoing } = makeHarness(db, BOT_INFO, config);
    await bot.handleUpdate(commandUpdate(1, 'settings'));
    expect(outgoingTexts(outgoing, 'sendMessage')[0]).toContain('Ajustes');
    expect(outgoing.filter((c) => c.method === 'editMessageText')).toHaveLength(0);
  });

  it('cambiar de idioma persiste y repinta en el idioma nuevo', async () => {
    const { bot, outgoing } = makeHarness(db, BOT_INFO, config);
    await bot.handleUpdate(commandUpdate(1, 'settings'));
    await bot.handleUpdate(callbackUpdate(2, 'set:l:en', 500));
    expect(getUserByTelegramId(db, 111)?.locale).toBe('en');
    expect(outgoingTexts(outgoing, 'editMessageText').at(-1)).toContain('Settings');
  });

  it('cambiar la unidad no convierte nada y persiste', async () => {
    const { bot } = makeHarness(db, BOT_INFO, config);
    await bot.handleUpdate(commandUpdate(1, 'settings'));
    await bot.handleUpdate(callbackUpdate(2, 'set:u:lb', 500));
    expect(getUserByTelegramId(db, 111)?.weightUnit).toBe('lb');
  });

  it('cambiar el salto persiste y los botones de peso lo reflejan', async () => {
    const { bot, outgoing } = makeHarness(db, BOT_INFO, config);
    await bot.handleUpdate(commandUpdate(1, 'settings'));
    await bot.handleUpdate(callbackUpdate(2, 'set:s:5', 500));
    expect(getUserByTelegramId(db, 111)?.weightStep).toBe(5);
    expect(lastKeyboardDatas(outgoing, 'editMessageText')).toContain('set:s:5');
  });

  it('ignora un valor no permitido sin escribir en la BD', async () => {
    const { bot } = makeHarness(db, BOT_INFO, config);
    await bot.handleUpdate(commandUpdate(1, 'settings'));
    await bot.handleUpdate(callbackUpdate(2, 'set:s:3', 500));
    await bot.handleUpdate(callbackUpdate(3, 'set:l:fr', 500));
    const user = getUserByTelegramId(db, 111);
    expect(user?.weightStep).toBe(2.5);
    // Sigue en 'es' (el language_code del harness, Task 4 Step 5): ningún handler
    // atendió 'set:l:fr' ni 'set:s:3', así que no se escribió nada.
    expect(user?.locale).toBe('es');
  });
});
```

Los textos esperados en español dependen de que el harness declare `language_code: 'es'` (Task 4, Step 5). Si esa parte no está hecha, estos tests verán inglés.

- [ ] **Step 2: Ejecutar y ver que falla**

Run: `pnpm vitest run apps/server/src/bot/settings.test.ts`
Expected: FAIL — `./settings` no existe.

- [ ] **Step 3: Añadir el espacio de callbacks**

En `apps/server/src/bot/callback-data.ts`, dentro de `CB`:

```ts
  // Espacio de /settings. Como wc:*, estos NO pasan por parseCallback: sus handlers
  // usan filtros por cadena exacta y se registran ANTES del catch-all de capture.ts.
  setLocale: (locale: string): string => `set:l:${locale}`,
  setUnit: (unit: string): string => `set:u:${unit}`,
  setStep: (step: number): string => `set:s:${step}`,
```

- [ ] **Step 4: Implementar la pantalla**

Crea `apps/server/src/bot/settings.ts`:

```ts
import { type Locale, type WeightUnit, updateUserPreferences } from '@gym-tracker/db';
import { type Bot, InlineKeyboard } from 'grammy';
import type { DatabaseSync } from 'node:sqlite';
import { setCurrent } from '../i18n/current';
import { LOCALES, LOCALE_NAMES } from '../i18n/index';
import { CB } from './callback-data';
import type { CustomContext } from './context';
import { T } from './texts';
import type { Rendered } from './welcome';

export const WEIGHT_STEPS = [1, 2.5, 5, 10] as const;
const UNITS: readonly WeightUnit[] = ['kg', 'lb'];

export interface SettingsPrefs {
  locale: Locale;
  unit: WeightUnit;
  step: number;
}

const mark = (active: boolean, label: string): string => (active ? `✓ ${label}` : label);

export function renderSettings(prefs: SettingsPrefs): Rendered {
  const keyboard = new InlineKeyboard();
  // row() ANTES de cada fila, nunca después: ver el comentario de exercise-picker.ts.
  for (const locale of LOCALES) {
    keyboard.text(mark(locale === prefs.locale, LOCALE_NAMES[locale]), CB.setLocale(locale));
  }
  keyboard.row();
  for (const unit of UNITS) {
    keyboard.text(mark(unit === prefs.unit, unit), CB.setUnit(unit));
  }
  keyboard.row();
  for (const step of WEIGHT_STEPS) {
    keyboard.text(mark(step === prefs.step, String(step)), CB.setStep(step));
  }
  keyboard.row().text(T.welcomeBackButton, CB.wcBack);

  const text = [
    T.settingsTitle,
    '',
    `${T.settingsLanguageLabel}: ${LOCALE_NAMES[prefs.locale]}`,
    `${T.settingsUnitLabel}: ${prefs.unit}`,
    `${T.settingsStepLabel}: ${prefs.step}`,
    '',
    T.settingsIntro,
  ].join('\n');

  return { text, keyboard };
}

const prefsOf = (ctx: CustomContext): SettingsPrefs => ({
  locale: ctx.user.locale,
  unit: ctx.user.weightUnit,
  step: ctx.user.weightStep,
});

// Sin parámetro de config: el botón ‹ Volver reutiliza el handler de CB.wcBack que
// ya registra welcome.ts, y es ese quien necesita el timezone.
export function registerSettings(bot: Bot<CustomContext>, db: DatabaseSync): void {
  // Mensaje NUEVO, como /help: /settings funciona con un entrenamiento en curso y
  // no debe pisar el mensaje activo de la sesión.
  bot.command('settings', async (ctx) => {
    const { text, keyboard } = renderSettings(prefsOf(ctx));
    await ctx.reply(text, { reply_markup: keyboard });
  });

  // Guarda, actualiza el estado global del update en curso y repinta EN EL SITIO,
  // así el cambio de idioma se ve al instante en la propia pantalla.
  const apply = async (ctx: CustomContext, patch: Partial<SettingsPrefs>): Promise<void> => {
    const next = { ...prefsOf(ctx), ...patch };
    updateUserPreferences(db, ctx.user.id, {
      locale: patch.locale,
      weightUnit: patch.unit,
      weightStep: patch.step,
    });
    setCurrent({ locale: next.locale, unit: next.unit, step: next.step });
    const { text, keyboard } = renderSettings(next);
    await ctx.editMessageText(text, { reply_markup: keyboard }).catch(() => {});
    await ctx.answerCallbackQuery(T.settingsSaved);
  };

  for (const locale of LOCALES) {
    bot.callbackQuery(CB.setLocale(locale), (ctx) => apply(ctx, { locale }));
  }
  for (const unit of UNITS) {
    bot.callbackQuery(CB.setUnit(unit), (ctx) => apply(ctx, { unit }));
  }
  for (const step of WEIGHT_STEPS) {
    bot.callbackQuery(CB.setStep(step), (ctx) => apply(ctx, { step }));
  }
}
```

Registrar handlers **por valor permitido** (un `callbackQuery` por opción) es lo que hace que `set:s:3` o `set:l:fr` no encuentren handler y no escriban nada: no hace falta validar a mano. El `catch-all` de `capture.ts` los recibirá y responderá con `sessionEndedToast`/nada, que es el comportamiento correcto para un botón inventado.

Si `Rendered`, `renderWelcome` o `welcomeModel` no están exportados en `welcome.ts` con esos nombres, ajusta el import; `Rendered` sí lo está (`welcome.ts:20`). Quita del import lo que no uses.

- [ ] **Step 5: Registrar en `bot.ts`**

Junto a `registerWelcome(bot, db, config);`, y **antes** de `registerCapture`:

```ts
  registerSettings(bot, db);
```

- [ ] **Step 6: Ejecutar y ver que pasa**

Run: `pnpm vitest run apps/server/src/bot/settings.test.ts && pnpm --filter @gym-tracker/server typecheck`
Expected: PASS los 7 tests.

- [ ] **Step 7: Commit**

```bash
git add apps/server/src/bot/settings.ts apps/server/src/bot/settings.test.ts apps/server/src/bot/callback-data.ts apps/server/src/bot/bot.ts
git commit -m "feat(server): add /settings screen for language, unit and step

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 9: Detección del idioma en el alta, menú ☰ y aviso en la bienvenida

**Files:**
- Modify: `apps/server/src/bot/auth.ts`
- Modify: `apps/server/src/bot/welcome.ts:85-105,182-192`
- Modify: `apps/server/src/main.ts:38`
- Test: `apps/server/src/bot/middlewares.test.ts`, `apps/server/src/bot/welcome.test.ts`

**Interfaces:**
- Consumes: `createUser` con `locale` (Task 1); `LOCALES`, `Locale` (Task 2); `T.commandDescription`, `T.welcomeSettingsHint` (Task 4).
- Produces:
  - `detectLocale(languageCode: string | undefined): Locale` exportada desde `auth.ts`
  - `botCommands(locale: Locale): Array<{ command: string; description: string }>`
  - `setBotCommands(api: Api): Promise<void>` — ahora llama a `setMyCommands` una vez por idioma.

- [ ] **Step 1: Escribir los tests que fallan**

En `apps/server/src/bot/middlewares.test.ts`:

```ts
describe('detectLocale', () => {
  it('solo el español da es; todo lo demás cae en inglés', () => {
    expect(detectLocale('es')).toBe('es');
    expect(detectLocale('es-MX')).toBe('es');
    expect(detectLocale('es-419')).toBe('es');
    expect(detectLocale('en')).toBe('en');
    expect(detectLocale('pt-BR')).toBe('en');
    expect(detectLocale('fr')).toBe('en');
    expect(detectLocale(undefined)).toBe('en');
    expect(detectLocale('')).toBe('en');
  });
});

it('da de alta al usuario con el idioma de su Telegram', async () => {
  const { bot } = makeHarness(db, BOT_INFO, config);
  // textUpdate del harness no manda language_code: hay que construir el update.
  await bot.handleUpdate({
    update_id: 1,
    message: {
      message_id: 10,
      date: 0,
      chat: { id: 111, type: 'private' },
      from: { id: 111, is_bot: false, first_name: 'U', language_code: 'pt-BR' },
      text: 'hola',
    },
  } as never);
  expect(getUserByTelegramId(db, 111)?.locale).toBe('en');
});
```

En `apps/server/src/bot/welcome.test.ts`:

```ts
it('menciona /settings en la bienvenida', () => {
  setCurrent({ locale: 'es', unit: 'kg', step: 2.5 });
  expect(renderWelcome(model).text).toContain('/settings');
});

it('botCommands traduce las descripciones e incluye settings', () => {
  expect(botCommands('en').map((c) => c.command)).toContain('settings');
  expect(botCommands('en').find((c) => c.command === 'help')?.description).toBe('How it works');
  expect(botCommands('es').find((c) => c.command === 'help')?.description).toBe('Cómo funciona');
});
```

- [ ] **Step 2: Ejecutar y ver que fallan**

Run: `pnpm vitest run apps/server/src/bot/middlewares.test.ts apps/server/src/bot/welcome.test.ts`
Expected: FAIL — `detectLocale` y `botCommands` no existen.

- [ ] **Step 3: Detectar el idioma en `auth.ts`**

```ts
import { type Locale, createUser, getUserByTelegramId } from '@gym-tracker/db';

/**
 * Idioma inicial a partir del `language_code` de Telegram. Solo el español tiene
 * catálogo propio además del inglés, y el INGLÉS es la reserva (decisión del
 * autor): quien llega con 'pt', 'fr' o sin código ve el bot en inglés.
 * El prefijo cubre 'es-MX', 'es-419' y compañía.
 */
export function detectLocale(languageCode: string | undefined): Locale {
  return languageCode !== undefined && languageCode.toLowerCase().startsWith('es') ? 'es' : 'en';
}
```

Y en el middleware:

```ts
    const user =
      getUserByTelegramId(db, fromId) ??
      createUser(db, {
        telegramUserId: fromId,
        timezone: config.timezone,
        locale: detectLocale(ctx.from?.language_code),
        createdAt: Date.now(),
      });
```

- [ ] **Step 4: Aviso en la bienvenida y comandos traducidos**

En `renderWelcome` (`welcome.ts:85-105`), añade la pista antes del bloque de métricas:

```ts
  const text = [
    escapeHtml(T.welcomeGreeting(model.firstName)),
    '',
    T.welcomeIntro,
    '',
    T.welcomeSettingsHint,
    '',
    metricsBlock(model.overview),
  ].join('\n');
```

Sustituye `BOT_COMMANDS` y `setBotCommands` (líneas 181-192) por:

```ts
/** El menú ☰ de Telegram (spec §6). El orden es el de la tabla del spec. */
const COMMAND_ORDER = ['start', 'finish', 'routines', 'last', 'help', 'settings'] as const;

export function botCommands(locale: Locale): Array<{ command: string; description: string }> {
  // Las descripciones salen del catálogo del idioma pedido, no del estado global:
  // setBotCommands registra el menú de TODOS los idiomas de una vez, al arrancar.
  return withCurrent({ locale, unit: 'kg', step: 2.5 }, () =>
    COMMAND_ORDER.map((command) => ({ command, description: T.commandDescription(command) })),
  );
}

/**
 * Registra el menú una vez por idioma. La llamada SIN language_code define el
 * menú por defecto, y por eso lleva el inglés: es el idioma de reserva.
 */
export async function setBotCommands(api: Api): Promise<void> {
  await api.setMyCommands(botCommands('en'));
  await api.setMyCommands(botCommands('es'), { language_code: 'es' });
}
```

Con `import { withCurrent } from '../i18n/current';` y `import type { Locale } from '@gym-tracker/db';`.

Si algún fichero importaba `BOT_COMMANDS`, cámbialo a `botCommands(...)` (`grep -rn "BOT_COMMANDS" apps`).

- [ ] **Step 5: Inicializar i18next en el arranque**

En `apps/server/src/main.ts`, después de `const config = loadConfig();`:

```ts
  initI18n(); // antes de crear el bot: cualquier handler puede pintar texto
```

con `import { initI18n } from './i18n/index';`.

- [ ] **Step 6: Ejecutar todo**

Run: `pnpm test && pnpm typecheck`
Expected: PASS completo, los cuatro workspaces incluidos. Este es el punto donde `pnpm typecheck` vuelve a estar limpio por primera vez desde la Task 1.

- [ ] **Step 7: Commit**

```bash
git add apps/server/src
git commit -m "feat(server): detect locale on signup and localize the command menu

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 10: Prueba de extremo a extremo en inglés y documentación

**Files:**
- Modify: `apps/server/src/bot/flows.test.ts`
- Modify: `CONTRIBUTING.md`
- Modify: `SPEC.md`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: nada de código.

- [ ] **Step 1: Escribir el flujo completo en inglés**

En `apps/server/src/bot/flows.test.ts`, siguiendo el estilo de los flujos que ya hay:

```ts
it('un usuario en inglés con libras registra una serie de principio a fin', async () => {
  const { bot, outgoing } = makeHarness(db, BOT_INFO, config);
  // Alta + preferencias: en/lb/5.
  await bot.handleUpdate(commandUpdate(1, 'settings'));
  await bot.handleUpdate(callbackUpdate(2, 'set:l:en', 500));
  await bot.handleUpdate(callbackUpdate(3, 'set:u:lb', 500));
  await bot.handleUpdate(callbackUpdate(4, 'set:s:5', 500));

  await bot.handleUpdate(commandUpdate(5, 'start'));
  expect(outgoingTexts(outgoing, 'sendMessage').at(-1)).toContain('/settings');

  await bot.handleUpdate(callbackUpdate(6, 'free', 501));
  await bot.handleUpdate(textUpdate(7, 'squat 100x5'));

  const active = outgoingTexts(outgoing, 'editMessageText').at(-1) ?? '';
  expect(active).toContain('Barbell squat'); // nombre traducido por name_key
  expect(active).toContain('lb'); // etiqueta de unidad, sin conversión
  expect(active).toContain('100×5'); // el número es el que escribió
  expect(lastKeyboardDatas(outgoing, 'editMessageText')).toContain('w+');

  await bot.handleUpdate(commandUpdate(8, 'finish'));
  expect(outgoingTexts(outgoing, 'editMessageText').at(-1)).toContain('Workout finished');
});
```

- [ ] **Step 2: Ejecutar y ver que pasa**

Run: `pnpm vitest run apps/server/src/bot/flows.test.ts`
Expected: PASS. Si el botón de peso no aparece rotulado `+5`, revisa que `capture.ts` use `weightStep()` (Task 6, Step 4).

- [ ] **Step 3: Documentar**

En `CONTRIBUTING.md`, añade una sección corta:

```markdown
## i18n

Los textos del bot viven en `apps/server/src/i18n/locales/{es,en}.ts` y se resuelven con
`i18next`. El idioma, la unidad y el salto de peso del update en curso están en el módulo
global `apps/server/src/i18n/current.ts`, que rellena el middleware `bot/preferences.ts`.

**Regla obligatoria:** cualquier código que genere texto FUERA del ciclo de vida de un update
(un `setTimeout`, un cron, un webhook, una tarea diferida) debe capturar `snapshot()` cuando se
programa y restaurarlo con `withCurrent()` cuando se ejecuta. `bot/rest-timer.ts` es el ejemplo.
Sin eso, el mensaje puede salir en el idioma de otro usuario o de otro update.

Añadir un idioma: un fichero nuevo en `locales/`, una entrada en `LOCALES` (`i18n/index.ts`) y
ampliar el `CHECK` de `users.locale` con una migración. El test `i18n/parity.test.ts` obliga a
traducir el catálogo entero.

Cambiar de unidad NO convierte ningún peso: es solo una etiqueta.
```

En `SPEC.md`, añade `/settings` a la tabla de comandos de §6 y una nota en la sección de datos
diciendo que `weight_kg` guarda el número tal cual lo escribió el usuario, en la unidad que
tuviera configurada entonces, y que el histórico puede mezclar unidades.

- [ ] **Step 4: Verificación final**

Run: `pnpm test && pnpm typecheck && pnpm --filter @gym-tracker/web build`
Expected: todo en verde. Anota el número de tests y de ficheros para el resumen (main venía de 276 tests en 43 ficheros).

- [ ] **Step 5: Commit**

```bash
git add apps/server/src/bot/flows.test.ts CONTRIBUTING.md SPEC.md
git commit -m "test(server): cover the English pound-unit flow end to end

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Pendiente de prueba manual

Los tests con harness no ven el bot real. Después de la Task 10, con un token de verdad
(`pnpm --filter @gym-tracker/server start`), comprueba a mano:

1. `/settings` pinta las tres filas y el `✓` salta de botón al pulsar.
2. Cambiar a English repinta la pantalla al instante, en inglés.
3. El menú ☰ de Telegram aparece traducido (puede tardar: Telegram lo cachea en el cliente).
4. Con `lb` y salto 5, los botones dicen `−5` / `+5` y un peso ya guardado sigue mostrando su
   número original.
5. El aviso de descanso llega en el idioma que estaba activo al registrar la serie.

## Riesgos anotados, no abordados

1. **Histórico con unidades mezcladas** — sin conversión, un agregado que cruce el cambio de
   unidad suma valores heterogéneos (tonelaje 30 días, récords, 1RM, estancamiento).
2. **Inglés como reserva** — quien llega con Telegram en portugués o francés ve inglés.
3. **Orden alfabético del selector** — las listas se ordenan por el nombre español de la BD
   (`ORDER BY name COLLATE NOCASE`), así que en inglés el orden no es alfabético.
4. **`apps/web` sigue en español con kg fijo**, y las 17 etiquetas de grupo muscular están
   duplicadas entre `packages/core` y el catálogo del bot.
